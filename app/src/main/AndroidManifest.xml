<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medical">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.Accessibility"/>
        <activity android:name=".UI.CreateEvent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.ConfirmEmail" />
        <activity android:name=".UI.Registration" />
        <activity android:name=".UI.ListOfEvents" />
        <activity android:name=".UI.ResetPassword" />
        <activity android:name=".UI.Login" />
        <activity android:name=".UI.Calender" />
        <activity android:name=".UI.EditEvent" />
        <activity android:name=".UI.homePage" />
    </application>

</manifest>